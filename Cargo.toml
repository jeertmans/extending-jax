[package]
name = "rms_norm"
version = "0.5.0"
edition = "2021"

[lib]
name = "_rms_norm"
crate-type = ["cdylib"]

[dependencies]
pyo3 = {version = "0.22", features = ["auto-initialize"]}
xla = { path = "xla-rs", features = ["pyo3"] }

[features]
extension-module = ["pyo3/extension-module"]